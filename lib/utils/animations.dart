class AppAnimations {
  static const Duration kShort = Duration(milliseconds: 300);
  static const Duration kMedium = Duration(milliseconds: 600);
  static const Duration kLong = Duration(milliseconds: 1000);

  static const scaleTween = 1.15;
  static const rotationTween = 0.08;
  static const opacityTween = 0.7;
  static const shakeOffset = 2.0;
}
